# Story 1.1: Monorepo Setup

## Status
Ready for Review

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [lint, typecheck, build]

## Story

**As a** developer,
**I want** a monorepo structure with Turborepo, npm workspaces, and shared packages,
**so that** all apps (desktop, web, worker) share types, components, and configurations.

## Acceptance Criteria

1. Monorepo root with `turbo.json` configured for build, lint, typecheck, dev pipelines
2. `apps/web` — Next.js 15 (App Router) project initialized with TypeScript, Tailwind CSS 4, shadcn/ui
3. `apps/desktop` — Tauri 2 project initialized with React + Vite frontend
4. `packages/shared` — Shared TypeScript types package with all data model interfaces (Event, Lecture, Speaker, UserProfile, EventParticipant, LectureAccess, ProcessingJob)
5. `packages/ui` — Shared React component library package (empty, ready for components)
6. `packages/supabase-client` — Supabase client wrapper package (browser + server clients)
7. `tsconfig.base.json` na raiz com paths configurados para `@scribia/shared`, `@scribia/ui`, `@scribia/supabase-client`
8. `.env.example` com todas as variáveis necessárias documentadas
9. `npm run dev`, `npm run build`, `npm run lint`, `npm run typecheck` funcionam na raiz
10. ESLint + Prettier configurados com config compartilhada

## Tasks / Subtasks

- [x] Task 1: Inicializar monorepo (AC: 1)
  - [x] Criar `package.json` raiz com workspaces: `apps/*`, `packages/*`
  - [x] Instalar Turborepo: `npm i -D turbo`
  - [x] Configurar `turbo.json` com pipelines: build, lint, typecheck, dev
- [x] Task 2: Setup apps/web — Next.js (AC: 2)
  - [x] `npx create-next-app@latest apps/web --typescript --tailwind --app --src-dir`
  - [x] Instalar shadcn/ui: `npx shadcn@latest init`
  - [x] Configurar alias para `@scribia/shared`, `@scribia/ui`
- [x] Task 3: Setup apps/desktop — Tauri (AC: 3)
  - [x] `npm create tauri-app@latest apps/desktop` com frontend React + Vite
  - [x] Configurar `tauri.conf.json` com app name "ScribIA Capture"
  - [x] Adicionar dependências Rust: `cpal`, `opus`, `reqwest`
- [x] Task 4: Setup packages/shared (AC: 4)
  - [x] Criar package com TypeScript interfaces de todos os data models
  - [x] Exportar types: Event, Lecture, Speaker, UserProfile, EventParticipant, LectureAccess, ProcessingJob
  - [x] Exportar constants: status enums, role enums
- [x] Task 5: Setup packages/ui (AC: 5)
  - [x] Criar package vazio com React + TypeScript
  - [x] Configurar exports no package.json
- [x] Task 6: Setup packages/supabase-client (AC: 6)
  - [x] Instalar `@supabase/supabase-js`, `@supabase/ssr`
  - [x] Criar `createBrowserClient()` e `createServerClient()`
  - [x] Exportar typed client com Database types
- [x] Task 7: Configuração compartilhada (AC: 7, 8, 9, 10)
  - [x] `tsconfig.base.json` com paths aliases
  - [x] ESLint config compartilhada
  - [x] Prettier config
  - [x] `.env.example` com todas as variáveis
  - [x] Verificar: `npm run dev`, `npm run build`, `npm run lint`, `npm run typecheck`

## Dev Notes

**Estrutura de diretórios alvo:**
```
scribia/
├── apps/
│   ├── desktop/          # Tauri 2 + React + Vite
│   └── web/              # Next.js 15 + App Router
├── packages/
│   ├── shared/           # @scribia/shared (types, constants)
│   ├── ui/               # @scribia/ui (React components)
│   └── supabase-client/  # @scribia/supabase-client
├── supabase/             # (criado na Story 1.2)
├── turbo.json
├── tsconfig.base.json
├── package.json
└── .env.example
```

**Variáveis de ambiente (.env.example):**
```
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=
ANTHROPIC_API_KEY=
AI_PROVIDER=gemini
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

**TypeScript path aliases:**
- `@scribia/shared` → `packages/shared/src`
- `@scribia/ui` → `packages/ui/src`
- `@scribia/supabase-client` → `packages/supabase-client/src`

**Tauri 2 prerequisites:**
- Windows: WebView2 runtime (built into Win 10/11)
- macOS: Xcode Command Line Tools
- Rust 1.70+

### Testing
- Vitest configurado nos packages
- Test: cada package compila sem erros
- Test: imports cross-package funcionam (`import { Event } from '@scribia/shared'`)
- Test: `turbo run build` completa sem erros

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-18 | 0.1.0 | Initial story creation | Morgan (PM) |
| 2026-03-19 | 0.2.0 | Add GEMINI_API_KEY, AI_PROVIDER env vars (ADR-001) | Aria (Architect) |
| 2026-03-19 | 1.0.0 | Implementation complete — all 7 tasks done, 15/15 turbo tasks pass | Dex (Dev) |

## Dev Agent Record

### Agent Model Used
Claude Opus 4.6 (1M context)

### Debug Log References
- Fixed `CookieMethods` → `CookieMethodsServer` in `@supabase/ssr` v0.6.x
- Rust installed via rustup: rustc 1.94.0

### Completion Notes List
- Monorepo initialized with Turborepo 2.8.19 + npm workspaces
- Next.js 16.2.0 (App Router) with React 19, Tailwind CSS 4
- Tauri 2 with React + Vite frontend, Rust deps: cpal, opus, reqwest
- @scribia/shared: 7 data model types + constants + path helpers
- @scribia/ui: empty shell ready for components
- @scribia/supabase-client: browser + server clients with SSR support
- All turbo commands pass: build ✅, typecheck ✅, lint ✅
- shadcn/ui v4 initialized with button component + utils
- Prettier configured (.prettierrc)
- Final validation: 15/15 turbo tasks pass (build + typecheck + lint)

### File List
- `package.json` — root monorepo config
- `turbo.json` — Turborepo pipeline config
- `tsconfig.base.json` — shared TypeScript config with path aliases
- `.env.example` — updated with GEMINI_API_KEY, AI_PROVIDER
- `.gitignore` — updated with Next.js, Turbo, Tauri, Supabase patterns
- `apps/web/` — Next.js 16 app (created via create-next-app)
- `apps/web/package.json` — updated with @scribia/* deps + typecheck script
- `apps/web/tsconfig.json` — updated with @scribia/* path aliases
- `apps/desktop/` — Tauri 2 app (created via create-tauri-app)
- `apps/desktop/package.json` — updated name, scripts, @scribia/* deps
- `apps/desktop/src-tauri/tauri.conf.json` — ScribIA Capture config
- `apps/desktop/src-tauri/Cargo.toml` — added cpal, opus, reqwest, tauri plugins
- `packages/shared/package.json` — @scribia/shared package config
- `packages/shared/tsconfig.json` — extends tsconfig.base
- `packages/shared/src/types.ts` — all data model interfaces
- `packages/shared/src/constants.ts` — enums, storage paths, progress checkpoints
- `packages/shared/src/index.ts` — re-exports
- `packages/ui/package.json` — @scribia/ui package config
- `packages/ui/tsconfig.json` — extends tsconfig.base with JSX
- `packages/ui/src/index.ts` — empty placeholder
- `packages/supabase-client/package.json` — @scribia/supabase-client config
- `packages/supabase-client/tsconfig.json` — extends tsconfig.base
- `packages/supabase-client/src/browser.ts` — createBrowserClient()
- `packages/supabase-client/src/server.ts` — createServerClient()
- `packages/supabase-client/src/index.ts` — re-exports

## QA Results
_To be filled by QA agent_
